export class User{

    name:string;
    color:string;
    options:[];

    constructor(name, color, options){
        this.name = name;
        this.color = color;
        this.options = options;

    }
    
}